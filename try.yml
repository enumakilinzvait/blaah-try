name:Test Project

on:
    pull_request:
        branches:
            -main
    
 jobs:
     test:
         runs-on:ubuntu-latest
         container:
         image:node20
        steps:
                 -uses: actions/checkout@v3
                 with;
                 node-versin: 20
                 -run:npm ci
                 -run:npm tests   
    
